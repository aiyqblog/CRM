# GitHub Issue 自动化所需配置
#
# 复制本文件为 .env 并填入真实值。
# .env 已在 .gitignore 中，不会被提交 —— 这是凭据唯一该待的地方。

# ── 个人访问令牌 ────────────────────────────────────────────────────────
# 生成位置：GitHub → Settings → Developer settings → Personal access tokens
# 推荐用「细粒度令牌」，需要的仓库权限：
#   Contents        Read and write   （建分支、推代码）
#   Issues          Read and write   （读 Issue、打标签、留评论）
#   Pull requests   Read and write   （开 PR）
#   Metadata        Read-only        （自动附带，不可取消）
#
# 注意：令牌等同于密码。不要贴进聊天记录、不要提交、不要写进任何文档。
GITHUB_TOKEN=

# ── 目标仓库 ────────────────────────────────────────────────────────────
# 形如 owner/repo。留空则从 git remote origin 的地址推断。
GITHUB_REPOSITORY=

# ── 自动化行为（可选，均有默认值）────────────────────────────────────────
# 每次运行最多处理几个 Issue。无人值守时不要调大：
# 堆量只会产生一堆需要人擦屁股的 PR。
# AGENT_MAX_ISSUES_PER_RUN=2

# E2E 使用的浏览器通道。Windows 本地用 chrome（系统已装，免下载）；
# Linux CI 用 chromium（Playwright 自带）。
# CRM_E2E_BROWSER_CHANNEL=chrome
