[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers --tb=short
markers =
    unit: 单元测试，不依赖数据库
    api: 接口测试，走 HTTP 层
    e2e: 端到端浏览器测试，需要 Playwright
filterwarnings =
    ignore::DeprecationWarning
