# 运行时依赖 —— 仅包含服务启动与业务功能所需
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.6
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.2
# EXIF 解析与图片压缩依赖（app/services/exif.py、storage.py）
# 曾经漏写这一条：本地能跑只是因为 easyocr 顺带装了 Pillow，
# 换台干净机器或进容器就会 ImportError。
pillow>=10.4
